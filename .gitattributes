# Recovery checksums cover original bytes, including historical line endings.
# Disable automatic CRLF/LF conversion for all frozen migration sources.
migration/** -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
